// this program allows the user to change the colors of an RGB LED and demonstrates the analog output function 
// Define pins for LEDs 
int ledR = 9;         
int ledG = 3;         
int ledB = 5;         

void setup() {
  // put your setup code here, to run once:
  pinMode(ledR, OUTPUT); // setup each variable as an output
  pinMode(ledG, OUTPUT);
  pinMode(ledB, OUTPUT);
}
void loop() {
  // put your main code here, to run repeatedly:
 // the value of each output combined to become a color on the color spectrum. 
 analogWrite(ledR, 155);
analogWrite(ledG, 6);
analogWrite(ledB, 125);
}
